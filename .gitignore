# Hugo build output
/public/
/resources/_gen/
/assets/jsconfig.json
hugo_stats.json

# Hugo generated files
.hugo_build.lock

# Node / npm (Hugo Blox installs npm deps for tailwind etc.)
# NOTE: package-lock.json is committed so CI can run `npm ci` and provide
# the Tailwind CLI binary that Hugo's css.tailwindcss function requires.
node_modules/

# OS / editor / tool-local config
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/
*.swp

# Local env
.env
.env.*
